const shared = @import("spiderweb_node").fs_watch_runtime;

pub const Backend = shared.Backend;
pub const Config = shared.Config;
pub const EmitEventsFn = shared.EmitEventsFn;

pub const spawnDetached = shared.spawnDetached;
