You are working in a Spiderweb-mounted folder.

Rules:
- Read `./AGENTS.md` first and follow it as mandatory workspace rules.
- Then do the task below using this mounted workspace.
- After the required reads listed in `AGENTS.md`, stop exploring and start implementation immediately unless a required service is genuinely missing.
- Treat the mounted namespace root as your current root.
- Write project files only under `nodes/local/fs/`.
- When creating or fixing a file, rewrite the whole file in one pass. If `game.py` needs a fix, replace it cleanly rather than appending repair fragments.
- If `nodes/local/fs/game.py` fails `python3 -m py_compile` or `python3 nodes/local/fs/game.py < nodes/local/fs/walkthrough.txt`, delete `nodes/local/fs/game.py` and recreate it from scratch before retrying.
- Preserve `nodes/local/fs/validate_game.py`.
- Use shell commands or small local scripts to create/edit files here. `apply_patch` is not available in this external Codex CLI run.

Task:
Build a Python terminal adventure game in the current working directory.

Required output files:
- `nodes/local/fs/game.py`
- `nodes/local/fs/game_manifest.json`
- `nodes/local/fs/walkthrough.txt`
- `nodes/local/fs/README.md`

Gameplay requirements:
- The game must run with stdin/stdout only.
- Use exactly the 10 locations from the workspace-visible shared data described in `./AGENTS.md` and listed in the manifest as `/shared_data/world_seed.json`.
- Use exactly the 10 items from the workspace-visible shared data described in `./AGENTS.md` and listed in the manifest as `/shared_data/items_seed.json`.
- Include at least the 3 puzzles from the workspace-visible shared data described in `./AGENTS.md` and listed in the manifest as `/shared_data/puzzle_seed.json`.
- The walkthrough must complete the game from a fresh start.
- The winning state must print this exact line:
  `VICTORY: Lantern of Nine Paths recovered`

Manifest requirements:
- `game_manifest.json` must be valid JSON.
- It must contain:
  - `title`: string
  - `locations`: array of 10 objects with `id` and `name`
  - `items`: array of 10 objects with `id` and `name`
  - `puzzles`: array with at least 3 objects with `id` and `name`
  - `shared_data_inputs`: array of absolute namespace paths
  - `victory_text`: exact victory line
  - `entrypoint`: `game.py`
  - `walkthrough`: `walkthrough.txt`
- `shared_data_inputs` must list:
  - `/shared_data/world_seed.json`
  - `/shared_data/items_seed.json`
  - `/shared_data/puzzle_seed.json`

Before you finish:
1. Run `python3 -m py_compile nodes/local/fs/game.py`
2. Run `python3 nodes/local/fs/game.py < nodes/local/fs/walkthrough.txt`
3. Run `python3 nodes/local/fs/validate_game.py --workspace nodes/local/fs --shared-data shared_data --output nodes/local/fs/game_validation.json`
4. If any step fails, fix the project files and rerun only the failed step(s)
5. In your final note, summarize which Spiderweb bootstrap actions you performed and which mounted paths you used

Important:
- Do not keep reading extra Spiderweb metadata once bootstrap is complete.
- Do not inspect unrelated files or wander the namespace before writing the required outputs.
- Do not use `find`, `rg --files`, or recursive `ls` on `services/`, `projects/`, or `meta/`.
- Do not stop after creating partial outputs. Finish only after `nodes/local/fs/README.md` exists and `nodes/local/fs/game_validation.json` has been written successfully.
