const ext = @import("ziggy-memory-store").ltm_store;

pub const LtmError = ext.LtmError;
pub const VersionedRecord = ext.VersionedRecord;
pub const VersionedMemStore = ext.VersionedMemStore;
pub const deinitRecords = ext.deinitRecords;
